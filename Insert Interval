class Solution {
    public int[][] insert(int[][] intervals, int[] newInterval) {

        ArrayList<int[]> res = new ArrayList<>();

        int i = 0;
        int n = intervals.length;

        // 1. Add intervals that are completely before newInterval
        while (i < n && intervals[i][1] < newInterval[0]) {
            res.add(intervals[i]);
            i++;
        }

        // 2. Merge all overlapping intervals with newInterval
        while (i < n && intervals[i][0] <= newInterval[1]) {

            newInterval[0] = Math.min(newInterval[0], intervals[i][0]);
            newInterval[1] = Math.max(newInterval[1], intervals[i][1]);

            i++;
        }

        // Add the merged newInterval
        res.add(newInterval);

        // 3. Add remaining intervals that are after newInterval
        while (i < n) {
            res.add(intervals[i]);
            i++;
        }

        return res.toArray(new int[res.size()][]);
    }
}

/* res naam ka ArrayList<int[]> banao.
i = 0 se intervals ko traverse karo.
Jab tak intervals[i][1] < newInterval[0]:
Current interval newInterval se completely pehle hai.
Current interval ko res mein add karo.
i++ karo.
Ab overlapping intervals ko process karo.
Jab tak intervals[i][0] <= newInterval[1]:
newInterval[0] ko minimum start se update karo.
newInterval[1] ko maximum end se update karo.
i++ karo.
Merged newInterval ko res mein add karo.
Ab bache hue saare intervals res mein directly add karo.
res ko int[][] mein convert karke return karo.
*/ 